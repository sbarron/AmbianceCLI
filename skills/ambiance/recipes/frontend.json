{
  "name": "frontend",
  "description": "Frontend code pattern analysis.",
  "defaults": {
    "subtree": "src",
    "maxFiles": 20,
    "format": "json"
  },
  "command": "frontend",
  "args": [
    "--json",
    "--project-path",
    "${projectPath}",
    "--subtree",
    "${subtree}",
    "--max-files",
    "${maxFiles}"
  ],
  "outputSchema": {
    "command": "string - always 'frontend' in JSON envelope mode",
    "exitCode": "number - CLI exit code (0 on success)",
    "success": "boolean - command success",
    "content": "array - formatted frontend analysis blocks",
    "metadata": "object - optional analysis metadata"
  },
  "notes": [
    "Does not require embeddings",
    "Automatically detects React, Vue, Angular, Svelte",
    "Use for UI-related queries and component analysis"
  ]
}
