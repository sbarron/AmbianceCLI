{
  "name": "summary",
  "description": "Single-file AST summary.",
  "defaults": {
    "format": "json"
  },
  "command": "summary",
  "args": ["${filePath}", "--json"],
  "outputSchema": {
    "command": "string - always 'summary' in JSON envelope mode",
    "exitCode": "number - CLI exit code (0 on success)",
    "success": "boolean - command success",
    "summary": "string - file summary output",
    "metadata": "object - optional symbol and analysis metadata"
  },
  "notes": [
    "Does not require embeddings",
    "Use for quick file overview without full context",
    "Shows public API surface of a module"
  ]
}
