{
  "name": "embeddings-status",
  "description": "Embedding status for a project.",
  "defaults": {
    "format": "json"
  },
  "command": "embeddings",
  "args": ["status", "--json", "--project-path", "${projectPath}"],
  "notes": [
    "Run before deep investigations to verify index readiness",
    "If stale, prefer context --auto-sync or embeddings update for incremental freshness",
    "Use embeddings create --force true only for migration/incompatible-model/corruption scenarios"
  ]
}
