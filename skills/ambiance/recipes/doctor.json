{
  "name": "doctor",
  "description": "Environment/readiness diagnostics.",
  "defaults": {
    "format": "json"
  },
  "command": "doctor",
  "args": ["--json"],
  "outputSchema": {
    "command": "string - always 'doctor' in JSON envelope mode",
    "exitCode": "number - CLI exit code (0 on success)",
    "success": "boolean - overall health status",
    "timestamp": "string - ISO timestamp",
    "node": "object - node version/platform/arch",
    "workspace": "object - detected project path and workspace env",
    "embeddings": "object - sqlite availability and embedding toggles",
    "dependencies": "object - runtime dependency probes"
  },
  "notes": [
    "Run this first to verify environment setup",
    "Use embeddings status for detailed index health",
    "Use to troubleshoot CLI issues"
  ]
}
