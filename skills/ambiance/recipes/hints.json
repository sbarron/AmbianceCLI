{
  "name": "hints",
  "description": "Project structure analysis and navigation hints.",
  "defaults": {
    "maxFiles": 100,
    "format": "json"
  },
  "command": "hints",
  "args": [
    "--json",
    "--project-path",
    "${projectPath}",
    "--max-files",
    "${maxFiles}"
  ],
  "outputSchema": {
    "command": "string - always 'hints' in JSON envelope mode",
    "exitCode": "number - CLI exit code (0 on success)",
    "success": "boolean - command success",
    "hints": "object|string - project hint payload (object in current implementation)",
    "type": "string - hint mode/type (optional)",
    "metadata": "object - analysis stats and file composition (optional)"
  },
  "notes": [
    "Does not require embeddings",
    "Use this first when exploring a new codebase",
    "maxFiles controls breadth: 50 (focused) to 200 (comprehensive)"
  ]
}
